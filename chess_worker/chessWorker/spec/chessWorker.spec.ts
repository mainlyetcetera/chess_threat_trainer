describe('the chess worker', () => {
  let w: null;

  beforeAll(() => {
    w = null;
  });

    it('can run a test', () => {
        expect(true).toBe(true);
    });

    it('exists', () => {
        expect(w).toBeTruthy();
    });
});
